# 修士配置开发记录

日期：2026-09-17；本次范围：H1 后台配置管理。

## 已交付

- `/cultivators`，统一主导航新增固定“修士”入口；页内六项导航：修士模板、职业成长、职业路线、灵根资质、境界配置、培养规则。
- 名称/编码搜索、分页、抽屉编辑；职业七维与成长分组、三个技能槽、逐级费用表、结构化还魂公式。
- 复用 hero_template、career/career_growth/career_skill、spiritual_root、realm、level_cost、i18n_text；新增 career_route、cultivator_rules（迁移0004）。
- GET/POST `/api/admin/cultivators`：开发管理会话鉴权、同源写入检查、字段校验、乐观锁、配置集行锁、父子表与翻译事务保存、实体前后快照及操作者审计；保存后旧审核失效。
- 修士/职业/灵根/境界引用校验、阶段约束、重复技能、境界重叠、升级费用、固定队伍及还魂参数检查。
- 复用配置中心校验/审核/发布；构建前再次校验三个技能、完整境界、两条筑基分支、结丹技能连续性。
- progression 模块新增 cultivatorConfigVersion、cultivatorRules、careerRoutes；明确 `cultivatorRuntimeEnabled:false`，没有玩家执行接口。

## 1.0 草稿初始化

执行 `pnpm cultivators:setup`，只用于 v1_0，不覆盖已有行，不修改D0或发布频道。重复运行新增0行。

- 四名修士：石岩、陆清、白灵、墨言。
- 六种灵根：杂/伪/三/双/天/异灵根；与物品统一六档无关。
- 炼气1–10、筑基11–20、结丹21–30。
- 四初始职业 + 八筑基分支 + 八结丹职业，共20个阶段职业、8条路线。
- 29档升级魂晶费用，累计到30级12805；突破、重置、还魂及灵息参数按PRD基线填充。

稳定编码映射（限定配置集，不改变D0含义）：

| 原编码 | v1_0 名称 | 对应修士 |
| --- | --- | --- |
| wu_xiu | 金刚禅修 | 石岩 |
| fa_xiu | 五行法修 | 陆清 |
| yi_xiu | 玄箓修 | 白灵 |
| qian_xiu | 剑修 | 墨言 |

初始基础七维及HP来自现有职业JSON；1.0成长率、路线成长、结丹增量来自成长数值分册§3.3–3.6。高阶职业沿用所属初始职业的Lv1基础，实际历史成长仍需未来实例账本实现。

## 明确边界和后续依赖

1. 当前v1_0尚未导入36个正式技能及其复杂效果，技能槽留空；页面显示发布待完善项，编译器拒绝发布未配足三个技能的职业。没有拿旧Demo技能冒充新技能。
2. 主线、试炼、借阵优惠和丹药编码为待绑定配置。任务权威状态、魂晶/灵石钱包、丹药实例库存未就绪；不存在可运行的玩家突破接口。
3. 培养规则将同版本突破公共费用、条件编码、还魂/灵息参数聚合到cultivator_rules JSON；路线存career_route。未单独建立突破规则表。升级费用唯一来源为level_cost，level明确为目标等级，读取和编译一致。
4. 保存允许缺少阶段技能的草稿，发布另做完整性校验。玩家操作启用门禁永久关闭于本阶段，不能因为修士配置发布就启用客户端扣款。
5. 现有模板的旧属性覆盖和技能覆盖字段不在本次编辑面板范围；更新基础模板字段不会清空它们。1.0新建模板不初始化旧覆盖字段。正式迁移前需核对旧覆盖与职业引用。
6. H2–H4仍待开发：玩家实例、成长账本、升级/还魂/突破/重置事务、任务凭据、Godot正式名册和旧档迁移。
7. 环境权限复用现有development配置会话；未实现正式多租户/按配置集授权。

## 验证

- `pnpm test:cultivators`：升级累计节点、参数边界、重复技能、路线阶段、引用错误事务回滚、并发修订冲突、审核失效、编译模块及不完整技能发布阻断。
- `pnpm test:items`：共用编译器改动后的物品回归。
- `pnpm typecheck`、`pnpm lint`、`pnpm build`。
- HTTP验证：匿名访问401；跨站写入403；合法开发身份读取草稿；旧修订写入409。
- 浏览器自动化连接当前不可用，未完成实际点击与视觉验收；沿用已验证的资源/物品页面布局及共享样式。

运行：先 `pnpm db:migrate`，然后 `pnpm cultivators:setup`，构建后 `pnpm resources:start`，打开 `/cultivators`。

## 形象与姓名解耦（2026-09-17）

模板编辑增加形象编码、姓名池、成长倾向、七维成长倍率；初始队伍增加独立固定姓名并允许复用模板。默认四模板改为原型名称，石岩/陆清/白灵/墨言保留在队伍姓名中。新增降魔禅修示例，与金刚禅修共享职业路线但成长不同。

当前金刚禅修沿用原职业成长；降魔禅修示例力道倍率1500、肉身850，其余1000，为待校准草稿值。形象编码已建立，美术头像仍待绑定。玩家实例与 C 端尚未实现随机姓名及模板成长消费；本次没有发布。

## 十二模板落库（2026-09-17）

已补齐四职业各1普通、2优秀，共12模板；分类和默认获取权重可编辑，列表显示同职业归一化概率。默认普通80、优秀各10；初始固定四人姓名与引用不变。玄箓符修调整为济世符修，青衫剑修调整为疾风剑修，保留稳定编码。

数据库迁移0006新增模板分类与权重；初始化重复运行无变更。成长倍率提供各模板方向的初始草稿，尚无技能方案与攻击频率实现，不会直接改变C端。

## 按模板配置百分比

修士模板页面增加“模板获取概率”，按职业同时编辑所有启用模板，精度0.01%，合计必须100%。不强制普通/优秀固定比例；0%不参与获取。PUT /api/admin/cultivators 接受配置集、配置集修订及完整职业概率组，使用整数基点（10000=100%）存入原权重字段。服务端检查合计、重复项、完整成员和并发修订，事务内写修订、审计并使旧审批失效。旧权重用最大余数法转换为百分比，总和保持100%；原全零池需手动分配。停用模板仍按原规则排除并重新归一化；固定初始队伍不受影响。
