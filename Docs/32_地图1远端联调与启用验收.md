# 地图1远端联调与启用验收

日期：2026-09-20。配置集v1_0，修订215。未发布频道。

## 数据与后台

- 修订213补齐出征规则、携带物品、食物和地图1费用；修订214启用53条记录（8敌人、13遭遇、31对象原型、1地图）。
- 修订215按策划36纠正旧出征配置：疗伤25%改35%，数据库记录变更前后审计。客户端同步向下取整、满血也占用本次疗伤机会。
- 地图1入山每人10灵息，最低携带20灵粮；基础负重60、力道系数2、体质系数1。食物只补灵粮。
- 地图后台增加出征规则与地图费用；敌群说明按实际新编组更新。原始地图文件的旧敌群文案不再覆盖数据库的新文案。
- 地图2—4及未接入敌人/遭遇保持停用。导入脚本遇到人工修改仍拒绝覆盖。

## 客户端适配

远端地图保留小数坐标、positionVersion、入口、动态阻挡及胜利状态。碰撞与标注优先读取远端regionsDocument；旧地图保留本地兼容。配置修订变化时重新建立导航。

未开启的新档与玩家技能配置不覆盖现有可玩预设/技能。资源服务生产配置保留旧客户端生产表的兼容路径，这不是新版生产客户端流程的完整验收。

## 验证

- 配置校验：修订215，0错误、0警告（14分册）。
- Admin：地图3项、遭遇5项测试通过；typecheck、lint、production build通过。
- Godot：地图闭环、正式战斗、正式地图、项目数据检查通过。
- 实际数据库编译6模块→Godot远端适配：31对象、36碰撞区域、17调整区域，坐标精度与远端几何修改生效。
- 实际出征→Boss胜利→拾取→归营，法器/真宝各1件进入装备库存。
- 疗伤35%、向下取整、同次不可重复、满血占用次数验证通过。
- 所有Godot测试使用--no-profile-write --ignore-config-cache，无真实存档写入。

复验命令：Admin执行tools/sync-expedition.ts（幂等），tools/activate-map01.ts默认回滚预演并输出编译测试包；Godot执行tools/validate_map01_remote.gd。启用工具只有--apply才修改数据库；不执行频道发布。

## 发布边界

开发频道仍dev-r4-5。此轮实现与草稿启用已完成，后续需同修订提交/审核/发布；客户端需包含此轮远端适配代码。新档NPC解锁、新版玩家技能、市场等开关继续关闭，不能据此宣称完整1.0已上线。装备初始倍率100/120及属性槽仍需平衡验收。
